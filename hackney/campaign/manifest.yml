---
applications:
  - name: campaign-site

    routes:
      - route: hackney-campaign-site.london.cloudapps.digital

    buildpacks:
      - nodejs_buildpack

    env:
      NODE_ENV: production
      USERNAME: ((username))
      PASSWORD: ((password))
